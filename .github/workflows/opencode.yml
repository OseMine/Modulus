name: opencode

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

# Loop protection: only one opencode run per issue/PR at a time, and a hard
# timeout so a runaway session cannot hang or spawn duplicate PRs.
concurrency:
  group: opencode-${{ github.event.issue.number || github.event.pull_request.number || github.run_id }}
  cancel-in-progress: false

jobs:
  # Rust/nih-plug specific job, stored as reusable actions (see
  # .github/actions/): formatting, clippy (-D warnings), full test suite.
  # Runs before the agent so OpenCode only sees a healthy tree.
  rust-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Set up Rust toolchain
        uses: ./.github/actions/setup

      - name: Run rust checks
        uses: ./.github/actions/checks

  opencode:
    if: |
      always() &&
      (contains(github.event.comment.body, '/oc') ||
       startsWith(github.event.comment.body, '/oc') ||
       contains(github.event.comment.body, '/opencode') ||
       startsWith(github.event.comment.body, '/opencode')) &&
      github.event.comment.user.type != 'Bot' &&
      github.event.comment.user.login != 'github-actions[bot]' &&
      github.event.comment.user.login != 'dependabot[bot]'
    needs: rust-check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Require write access
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const { login, type } = context.payload.comment.user;
            if (type === 'Bot') {
              core.setFailed(`Bot account ${login} cannot trigger opencode`);
              return;
            }
            try {
              const { data } = await github.rest.repos.getCollaboratorPermissionLevel({
                owner,
                repo,
                username: login
              });
              if (!['admin', 'write'].includes(data.permission)) {
                core.setFailed(`${login} needs write access to trigger opencode (got: ${data.permission})`);
              } else {
                core.info(`${login} has ${data.permission} access - OK`);
              }
            } catch (err) {
              core.setFailed(`Could not verify permissions for ${login}: ${err.message}`);
            }

      - name: Run opencode
        uses: anomalyco/opencode/github@latest
        env:
          OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
        with:
          model: opencode/deepseek-v4-flash-free
          use_github_token: true
          prompt: |
            Handle the triggering comment on this issue/pull request:

            ${{ github.event.comment.body }}

            Execute the requested task: diagnose and fix problems, implement
            the requested changes, and keep todo.md and reports/ up to date.
            If the task mentions "todo.md" or a GitHub issue, first check the
            open items there and work through the matching ones.

            Rules:
            - Never touch the workflows/actions in this repository unless the
              comment explicitly asks for it.
            - Prefer small, focused edits that follow the existing code style.
            - Update todo.md: mark fixed items with [x] and move completed
              items to archived-todo.md.
            - After implementing, run `cargo fmt --all` and `cargo clippy
              --workspace --all-targets -- -D warnings` and fix any issues.
            - Do not commit or push: the action commits the changes
              automatically.
