[package]
name = "modulus-fx"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Modulus FX — standalone multi-effects processor (VST3/CLAP)"

[lib]
crate-type = ["cdylib"]

[dependencies]
nih_plug = { workspace = true }
modulus-core = { workspace = true }
